{
    "name": "@moe-tech/orchestrator-activity",
    "version": "1.0.0",
    "scripts": {
        "prebuild": "cd src/library && npm i && tsc -d && cd ../functions && npm i",
        "build": "cd src/functions && tsc -d"
    }
}
